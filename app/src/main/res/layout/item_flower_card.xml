<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    app:cardCornerRadius="30dp"
    app:cardElevation="16dp"
    app:contentPadding="16dp"
    >

    <androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/vp2_images"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginVertical="16dp"
            android:layout_marginHorizontal="8dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tv_item_card_title"
            />

    <TextView
        android:id="@+id/tv_item_card_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Flower Title"
        android:textStyle="bold"
        android:textSize="24sp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toBottomOf="@id/vp2_images"
        app:layout_constraintStart_toStartOf="@id/vp2_images"
        android:layout_marginTop="16dp"
        />

    <TextView
        android:id="@+id/tv_item_card_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Description"
        android:textSize="18sp"
        android:layout_marginHorizontal="18dp"
        app:layout_constraintTop_toBottomOf="@id/tv_item_card_title"
        app:layout_constraintStart_toStartOf="@id/tv_item_card_title"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginVertical="8dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>
